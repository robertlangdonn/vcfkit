# Build artifacts
target/

# Cargo lock for libraries (keep for binaries)
# Cargo.lock  # keep this for the binary crate

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Benchmarks output
benches/e2e/report.md

# Git worktrees
.worktrees/

# AI assistant context (not for the repo)
_docs/

# Real-world reference data (downloaded on first run, not committed)
tests/real_world/

# Environment / secrets
.env
.env.*
*.env
